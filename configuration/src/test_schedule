def at(time):
    return time

add_resource('torrent', 'big-file.torrent')

on('router') \
    .schedule('run_bar', at(0), "bar") \
    .schedule('blah', at(1), "blah")

on('bob').schedule('run_foo', at(5), "foo")
on('bob').schedule('grr', at(2), "exec grr")

# vim: ft=python
