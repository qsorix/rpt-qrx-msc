"Reference - Model"
-------------------
FIXME: Przenieść do odpowiedniego rozdziału, uzupełnić i dokładnie opisać.

Funkcje i metody:
  - create_model(name)

  - add_host(name, attr1=value1, ...)
    zwraca Host

  - add_interface(name, attr1=value1, ...)
    zwraca Interface

  - add_link(interface, interface)
    zwraca Link

Typy:

  - Host
     - set_attributes
  - Interface
     - set_attributes
  - Link

#FIXME: schemat UML
  - Host
  - Interface (agregacja w Host)
  - Link (Klasa powiązania na połączeniu Interface-Interface)

Reference "Laboratory"
======================
FIXME: Przenieść do odpowiedniego rozdziału, uzupełnić i dokładnie opisać.

Funkcje i metody:
  - create_laboratory(name)

  - add_device(name, attr1=value1, ...)
    Zwraca Device

  - add_interface(name, attr1=value1, ...)
    Zwraca Interface

Typy:

  - Device
     - set_attributes
  - Interface
     - set_attributes

#FIXME: schemat UML
  - Device
  - Interface (agregacja w Device)

Reference "Mapping"
====================
FIXME: Przenieść do odpowiedniego rozdziału, uzupełnić i dokładnie opisać.

Funkcje i metody:
  - create_mapping(name)

  - bind(object1, object2)

#FIXME: schemat UML ?

Reference "Schedule"
====================
FIXME: Przenieść do odpowiedniego rozdziału, uzupełnić i dokładnie opisać.

Funkcje i metody:
  - create_schedule(name)
  - append_schedule(host, [(name, run_policy, command), ...])

Typy:
  - RunPolicy
  - Command

Reference "Global"
==================
parameters

Metody:
  parameters['key']
  parameters.get_secure('password')
