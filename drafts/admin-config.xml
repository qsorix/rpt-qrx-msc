<configuration>

  <hosts>
	  <host id="A">
			<ip>192.168.6.2</ip>
		</host>

		<host id="B">>
			<name>marvin</name>
		</host>
  </hosts>

  <test>
    <duration>15ms</duration>

		<resource id="torrent=test">
			<file>/local/path/plik.torrent</file>
		</resource>

    <schedule id="torrent-test">
      0: {torrent} ctorrent {torrent-file} -ble -sre
      10: kill {torrent}
    </schedule>

    <setup for="A">
      <file id="torrent-file"/>
      <schedule id="torrent-test"/>
    </setup>

    <setup for="B">
      <file id="torrent-file"/>
      <schedule id="torrent-test"/>
    </setup>
  </test>

</configuration>
